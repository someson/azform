// Command azform is a TUI wrapper around the Azure CLI that parses a partial
// `az` command, shows an interactive form for its parameters, and prints the
// completed command back to the shell.
package main
